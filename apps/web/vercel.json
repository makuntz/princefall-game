{
  "buildCommand": "npm run build --workspace=@princefall/game-core && npm run build --workspace=@princefall/shared && npm run build",
  "outputDirectory": "dist",
  "installCommand": "cd ../.. && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

